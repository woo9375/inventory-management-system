# Coding Rules — 호텔 재고 관리 시스템

> 상세 코딩 규약. `.agents/rules/05_code-conventions.md` 참조.
> 이 문서는 Rules의 핵심 내용을 요약하고 예시를 보강합니다.

---

## 빠른 참조

| 규칙 | 참조 |
|------|------|
| GAS 환경 제약 | `.agents/rules/01_gas-environment.md` |
| 상수는 Config.gs에만 | `.agents/rules/05_code-conventions.md` §1 |
| 모듈 헤더 필수 | `.agents/rules/05_code-conventions.md` §2 |
| 모듈별 책임 분리 | `.agents/rules/05_code-conventions.md` §3 |
| 에러 처리 패턴 | `.agents/rules/05_code-conventions.md` §5 |
| 네이밍 규약 | `.agents/rules/05_code-conventions.md` §6 |
| API 응답 패턴 | `.agents/rules/05_code-conventions.md` §7 |
| 배치 처리 원칙 | `.agents/rules/05_code-conventions.md` §8 |

## 커밋 메시지

```
[feat] 시즌 관리 UI 추가
[fix] FIFO 마감 시 로트 단가 누락 수정
[refactor] WebApp.gs API 핸들러 모듈 분리
[style] 대시보드 카드 레이아웃 개선
[docs] README 배포 가이드 업데이트
[chore] Migration v10 에러 로그 시트 추가
```
